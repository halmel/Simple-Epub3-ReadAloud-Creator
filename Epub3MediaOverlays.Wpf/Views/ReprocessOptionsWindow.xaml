<Window x:Class="Epub3MediaOverlays.Wpf.ReprocessOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Reprocess Options" Height="450" Width="550"
        WindowStartupLocation="CenterOwner"
        Background="#f5f5f5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Select Files to Delete for Reprocessing" FontSize="14" FontWeight="Bold" Foreground="#333"/>
        <TextBlock Grid.Row="1" Text="Choose which cached files to delete. Files that don't exist cannot be selected." 
                   FontSize="11" Foreground="Gray" TextWrapping="Wrap" Margin="0,5,0,10"/>

        <!-- Separator -->
        <Border Grid.Row="2" Height="1" Background="#ddd" Margin="0,10,0,10"/>

        <!-- File Options -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Transcript Path -->
                <Border BorderBrush="#e0e0e0" BorderThickness="1" Padding="12" CornerRadius="4" Background="White" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Transcript Cache" FontWeight="SemiBold" Foreground="#333" Margin="0,0,5,0"/>
                                <TextBlock x:Name="TranscriptStatus" Foreground="Gray" FontSize="10"/>
                            </StackPanel>
                            <TextBlock Text="Cached transcriptions (transcriptions.json)" Foreground="Gray" FontSize="10" Margin="0,2,0,0"/>
                            <TextBlock x:Name="TranscriptPath" Text="" Foreground="DarkSlateGray" FontSize="9" Margin="0,4,0,0" TextWrapping="Wrap"/>
                        </StackPanel>

                        <CheckBox x:Name="DeleteTranscript" Grid.Column="1" VerticalAlignment="Top" Margin="10,0,0,0"/>
                    </Grid>
                </Border>

                <!-- Alignment Path -->
                <Border BorderBrush="#e0e0e0" BorderThickness="1" Padding="12" CornerRadius="4" Background="White" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Alignment Cache &amp; Log" FontWeight="SemiBold" Foreground="#333" Margin="0,0,5,0"/>
                                <TextBlock x:Name="AlignmentStatus" Foreground="Gray" FontSize="10"/>
                            </StackPanel>
                            <TextBlock Text="Alignment data (Words.json) and log (AlingmentLog.json)" Foreground="Gray" FontSize="10" Margin="0,2,0,0"/>
                            <TextBlock x:Name="AlignmentPath" Text="" Foreground="DarkSlateGray" FontSize="9" Margin="0,4,0,0" TextWrapping="Wrap"/>
                        </StackPanel>

                        <CheckBox x:Name="DeleteAlignment" Grid.Column="1" VerticalAlignment="Top" Margin="10,0,0,0"/>
                    </Grid>
                </Border>

                <!-- Final EPUB Path -->
                <Border BorderBrush="#e0e0e0" BorderThickness="1" Padding="12" CornerRadius="4" Background="White" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Processed EPUB" FontWeight="SemiBold" Foreground="#333" Margin="0,0,5,0"/>
                                <TextBlock x:Name="FinalEpubStatus" Foreground="Gray" FontSize="10"/>
                            </StackPanel>
                            <TextBlock Text="Final generated EPUB file" Foreground="Gray" FontSize="10" Margin="0,2,0,0"/>
                            <TextBlock x:Name="FinalEpubPath" Text="" Foreground="DarkSlateGray" FontSize="9" Margin="0,4,0,0" TextWrapping="Wrap"/>
                        </StackPanel>

                        <CheckBox x:Name="DeleteFinalEpub" Grid.Column="1" VerticalAlignment="Top" Margin="10,0,0,0"/>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Separator -->
        <Border Grid.Row="4" Height="1" Background="#ddd" Margin="0,10,0,10"/>

        <!-- Warning Message -->
        <TextBlock Grid.Row="5" x:Name="WarningMessage" Text="" Foreground="#d32f2f" FontSize="10" TextWrapping="Wrap" Visibility="Collapsed" Margin="0,0,0,10"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancel" Click="Cancel_Click" Width="100" Background="#f0f0f0" Foreground="#333" Margin="0,0,10,0"/>
            <Button x:Name="ReprocessButton" Content="Delete &amp; Reprocess" Click="Reprocess_Click" Width="150" Background="#2196F3" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>

